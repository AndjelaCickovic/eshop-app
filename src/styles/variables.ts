export const theme: ITheme = {
  primaryColor: "#d87e0d",
  secondaryColor: "#2f2c2c",
  //TODO: Add other theme styling
  // --contrastColor:
  // --success,error,warning

  // --fontSize -sm, -md, -lg
  // --fontWeight -normal, -bold, -bolder
  // --spacing -sm -md -lg
};

interface ITheme {
  primaryColor: string;
  secondaryColor: string;
}
